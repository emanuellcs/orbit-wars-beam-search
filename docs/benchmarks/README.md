# Benchmark Reports

This directory is the default output location for `scripts/bench.py` reports and
baseline measurements. Each report records the date, seed set, budget, and
per-opponent win rates so results are reproducible and comparable across
changes.
